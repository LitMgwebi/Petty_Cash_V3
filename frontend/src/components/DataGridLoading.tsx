import { LinearProgress } from "@mui/material";
import { FC } from "react";

const DataGridLoading: FC = () => {
    return <LinearProgress sx={{ width: "100%" }} />
}

export default DataGridLoading;