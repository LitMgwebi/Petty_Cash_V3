export interface AccountSet {
    accountSetId: number;
    accountNumber: string;
    name: string;
    description: string;
}

export interface AccountSetAppend {
    accountNumber: string;
    name: string;
    description: string;
}
